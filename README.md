# 🛒 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Интернет-магазин

## Версия
**Версия:** 1.0
**Дата:** 27.01.2026
**Статус:** Черновик

---

## 1. ОБЩЕЕ ОПИСАНИЕ

### 1.1 Назначение системы
Система представляет собой полнофункциональный интернет-магазин для продажи товаров с возможностью:
- Просмотра каталога товаров
- Управления корзиной покупок
- Оформления заказов
- Управления учётными записями пользователей
- Админ-панели для управления контентом

### 1.2 Целевая аудитория
- Покупатели (клиенты магазина)
- Администраторы (менеджеры магазина)
- Модераторы (контент-менеджеры)

### 1.3 Технологический стек
**Backend:**
- FastAPI (Web Framework)
- SQLAlchemy (ORM)
- PostgreSQL (База данных)
- Pydantic (Валидация)
- JWT (Аутентификация)
- Pytest (Тестирование)

**Frontend:**
- HTML5, CSS3, JavaScript (ES6+)
- Собственный Event Bus
- Playwright (E2E тесты)

---

## 2. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 2.1 Модульная архитектура

Система состоит из независимых модулей, общающихся через Event Bus:

#### Модуль `users` - Управление пользователями
**Ответственности:**
- Регистрация новых пользователей
- Аутентификация и авторизация
- Управление профилями
- Управление ролями (buyer, admin, moderator)

**События:**
- Публикует: `user:registered`, `user:logged_in`, `user:profile_updated`
- Подписывается: `order:created` (для обновления истории)

---

#### Модуль `catalog` - Каталог товаров
**Ответственности:**
- Управление товарами (создание, редактирование, удаление)
- Управление категориями товаров
- Поиск и фильтрация товаров
- Просмотр детальной информации о товаре

**Сущности:**
- Товар (Product): id, название, описание, цена, количество, изображения, категория, характеристики
- Категория (Category): id, название, описание, родительская категория, изображение
- Характеристика (Specification): название, значение, тип данных

**События:**
- Публикует: `product:created`, `product:updated`, `product:deleted`
- Подписывается: - (нет зависимостей)

---

#### Модуль `cart` - Корзина покупок
**Ответственности:**
- Добавление товаров в корзину
- Изменение количества товаров
- Удаление товаров из корзины
- Очистка корзины
- Расчёт итоговой суммы

**Сущности:**
- Корзина (Cart): id, пользователь (если авторизован), товары, общая сумма
- Товар в корзине (CartItem): товар, количество, цена на момент добавления

**События:**
- Публикует: `cart:item_added`, `cart:item_removed`, `cart:cleared`
- Подписывается: `product:deleted` (для удаления недоступных товаров)

---

#### Модуль `orders` - Управление заказами
**Ответственности:**
- Оформление заказа из корзины
- Управление статусами заказов
- История заказов пользователя
- Отмена заказа

**Сущности:**
- Заказ (Order): id, пользователь, товары, сумма, статус, дата создания, дата обновления
- Статус заказа: pending, confirmed, processing, shipped, delivered, cancelled

**События:**
- Публикует: `order:created`, `order:status_changed`, `order:cancelled`
- Подписывается: `user:registered` (для переноса корзины), `payment:completed`

---

#### Модуль `payments` - Платежная система
**Ответственности:**
- Обработка платежей
- Интеграция с платёжными шлюзами
- Управление транзакциями

**Сущности:**
- Платёж (Payment): id, заказ, сумма, статус, метод оплаты, транзакция

**События:**
- Публикует: `payment:completed`, `payment:failed`, `payment:refunded`
- Подписывается: `order:created` (для инициации оплаты)

---

#### Модуль `reviews` - Отзывы и рейтинги
**Ответственности:**
- Добавление отзывов к товарам
- Рейтинг товаров
- Модерация отзывов

**Сущности:**
- Отзыв (Review): id, товар, пользователь, рейтинг, текст, статус модерации, дата

**События:**
- Публикует: `review:added`, `review:moderated`
- Подписывается: `user:registered`, `order:delivered`

---

#### Модуль `admin` - Админ-панель
**Ответственности:**
- Управление товарами и категориями
- Управление заказами
- Управление пользователями
- Аналитика и отчёты
- Модерация отзывов

**События:**
- Публикует: - (нет)
- Подписывается: Все события для логирования и аналитики

---

#### Модуль `notifications` - Уведомления
**Ответственности:**
- Email уведомления
- Внутренние уведомления
- Шаблоны уведомлений

**События:**
- Публикует: `notification:sent`
- Подписывается: `user:registered`, `order:status_changed`, `payment:completed`, `review:added`

---

## 3. API ENDPOINTS

### 3.1 Users Module
```
POST   /api/auth/register           - Регистрация
POST   /api/auth/login              - Вход
POST   /api/auth/logout             - Выход
POST   /api/auth/refresh            - Обновление токена

GET    /api/users/profile           - Получить профиль
PUT    /api/users/profile           - Обновить профиль
GET    /api/users/:id/orders        - История заказов пользователя
```

### 3.2 Catalog Module
```
GET    /api/products                - Список товаров (с пагинацией, фильтрацией)
GET    /api/products/:id            - Детали товара
POST   /api/products                - Создать товар (admin)
PUT    /api/products/:id            - Обновить товар (admin)
DELETE /api/products/:id            - Удалить товар (admin)

GET    /api/categories              - Список категорий
GET    /api/categories/:id          - Детали категории с товарами
POST   /api/categories              - Создать категорию (admin)
PUT    /api/categories/:id          - Обновить категорию (admin)
DELETE /api/categories/:id          - Удалить категорию (admin)

GET    /api/products/search         - Поиск товаров
```

### 3.3 Cart Module
```
GET    /api/cart                    - Получить корзину
POST   /api/cart/items              - Добавить товар в корзину
PUT    /api/cart/items/:id          - Изменить количество
DELETE /api/cart/items/:id          - Удалить товар из корзины
DELETE /api/cart                    - Очистить корзину
POST   /api/cart/checkout           - Оформить заказ
```

### 3.4 Orders Module
```
GET    /api/orders                  - Список заказов
GET    /api/orders/:id              - Детали заказа
POST   /api/orders                  - Создать заказ
PUT    /api/orders/:id/status       - Изменить статус (admin)
POST   /api/orders/:id/cancel       - Отменить заказ
```

### 3.5 Payments Module
```
POST   /api/payments/initiate       - Инициировать оплату
POST   /api/payments/callback       - Callback от платёжной системы
GET    /api/payments/:id/status     - Статус платежа
POST   /api/payments/:id/refund     - Возврат (admin)
```

### 3.6 Reviews Module
```
GET    /api/products/:id/reviews    - Отзывы на товар
POST   /api/products/:id/reviews    - Добавить отзыв
PUT    /api/reviews/:id             - Редактировать отзыв
DELETE /api/reviews/:id             - Удалить отзыв
POST   /api/reviews/:id/moderate    - Модерация (admin)
```

### 3.7 Admin Module
```
GET    /api/admin/dashboard         - Дашборд со статистикой
GET    /api/admin/analytics         - Аналитика и отчёты
```

---

## 4. СТРУКТУРА БАЗЫ ДАННЫХ

### 4.1 Таблицы модуля `users`
```
users
├── id (UUID, PK)
├── email (VARCHAR, UNIQUE, NOT NULL)
├── password_hash (VARCHAR, NOT NULL)
├── first_name (VARCHAR)
├── last_name (VARCHAR)
├── phone (VARCHAR)
├── role (ENUM: buyer, admin, moderator)
├── is_active (BOOLEAN, default: true)
├── created_at (TIMESTAMP)
└── updated_at (TIMESTAMP)
```

### 4.2 Таблицы модуля `catalog`
```
products
├── id (UUID, PK)
├── name (VARCHAR, NOT NULL)
├── description (TEXT)
├── price (DECIMAL, NOT NULL)
├── stock_quantity (INTEGER, default: 0)
├── category_id (UUID, FK → categories.id)
├── images (JSONB)  // массив URL
├── specifications (JSONB)  // ключ-значение характеристик
├── is_active (BOOLEAN, default: true)
├── created_at (TIMESTAMP)
└── updated_at (TIMESTAMP)

categories
├── id (UUID, PK)
├── name (VARCHAR, NOT NULL)
├── description (TEXT)
├── parent_id (UUID, FK → categories.id, nullable)
├── image_url (VARCHAR)
├── sort_order (INTEGER)
├── created_at (TIMESTAMP)
└── updated_at (TIMESTAMP)
```

### 4.3 Таблицы модуля `cart`
```
carts
├── id (UUID, PK)
├── user_id (UUID, FK → users.id, nullable)  // nullable для гостей
├── session_id (VARCHAR, nullable)  // для гостей
├── created_at (TIMESTAMP)
└── updated_at (TIMESTAMP)

cart_items
├── id (UUID, PK)
├── cart_id (UUID, FK → carts.id)
├── product_id (UUID, FK → products.id)
├── quantity (INTEGER, NOT NULL)
├── price (DECIMAL, NOT NULL)  // цена на момент добавления
└── added_at (TIMESTAMP)
```

### 4.4 Таблицы модуля `orders`
```
orders
├── id (UUID, PK)
├── user_id (UUID, FK → users.id)
├── total_amount (DECIMAL, NOT NULL)
├── status (ENUM: pending, confirmed, processing, shipped, delivered, cancelled)
├── shipping_address (JSONB)
├── contact_phone (VARCHAR)
├── created_at (TIMESTAMP)
└── updated_at (TIMESTAMP)

order_items
├── id (UUID, PK)
├── order_id (UUID, FK → orders.id)
├── product_id (UUID, FK → products.id)
├── product_name (VARCHAR)  // сохраняем на момент заказа
├── quantity (INTEGER, NOT NULL)
├── price (DECIMAL, NOT NULL)  // цена на момент заказа
└── subtotal (DECIMAL, NOT NULL)
```

### 4.5 Таблицы модуля `payments`
```
payments
├── id (UUID, PK)
├── order_id (UUID, FK → orders.id)
├── amount (DECIMAL, NOT NULL)
├── status (ENUM: pending, completed, failed, refunded)
├── payment_method (VARCHAR)
├── transaction_id (VARCHAR, UNIQUE)
├── metadata (JSONB)  // данные от платёжной системы
├── created_at (TIMESTAMP)
└── completed_at (TIMESTAMP)
```

### 4.6 Таблицы модуля `reviews`
```
reviews
├── id (UUID, PK)
├── product_id (UUID, FK → products.id)
├── user_id (UUID, FK → users.id)
├── rating (INTEGER, 1-5, NOT NULL)
├── text (TEXT)
├── moderation_status (ENUM: pending, approved, rejected)
├── created_at (TIMESTAMP)
└── updated_at (TIMESTAMP)
```

---

## 5. ПОЛЬЗОВАТЕЛЬСКИЕ СЦЕНАРИИ

### 5.1 Гостевой режим (без регистрации)

**Просмотр каталога:**
1. Пользователь открывает главную страницу
2. Видит список категорий
3. Может просматривать товары без авторизации
4. Может использовать поиск и фильтры

**Корзина для гостя:**
1. Гость добавляет товары в корзину
2. Корзина сохраняется в sessionStorage/localStorage
3. При оформлении заказа - предложение зарегистрироваться

---

### 5.2 Регистрация и вход

**Регистрация:**
1. Пользователь заполняет форму (email, пароль, имя)
2. Система валидирует данные
3. Отправляет email подтверждение (опционально)
4. Создаётся учётная запись с ролью "buyer"
5. Автоматический вход после регистрации

**Вход:**
1. Пользователь вводит email и пароль
2. Система проверяет учётные данные
3. Генерирует JWT access токен и refresh токен
4. Токены сохраняются в httpOnly cookies
5. Перенаправление на главную страницу

---

### 5.3 Покупка товара

**Добавление в корзину:**
1. Пользователь просматривает товар
2. Нажимает "Добавить в корзину"
3. Выбирает количество (опционально)
4. Товар добавляется в корзину
5. Показывается уведомление

**Оформление заказа:**
1. Пользователь открывает корзину
2. Проверяет список товаров и итоговую сумму
3. Нажимает "Оформить заказ"
4. Заполняет форму доставки (если первый раз)
5. Выбирает способ оплаты
6. Подтверждает заказ
7. Перенаправление на страницу оплаты
8. После оплаты - показывается страница успешного заказа

**Отслеживание заказа:**
1. Пользователь открывает "Мои заказы"
2. Видит список всех заказов со статусами
3. Может открыть детали заказа
4. Видит историю изменения статусов

---

### 5.4 Админ-панель

**Управление товарами:**
1. Администратор входит в систему
2. Открывает админ-панель
3. Переходит в раздел "Товары"
4. Может создавать, редактировать, удалять товары
5. Может управлять категориями
6. Может загружать изображения

**Управление заказами:**
1. Администратор видит список всех заказов
2. Может фильтровать по статусу
3. Меняет статус заказа
4. Видит детали заказа

---

## 6. НЕФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 6.1 Производительность
- Время ответа API: < 200мс для 95% запросов
- Время загрузки страницы: < 2 секунды
- Поддержка 1000+ одновременных пользователей

### 6.2 Безопасность
- Все пароли хешируются (bcrypt/argon2)
- JWT токены с коротким временем жизни (15 мин)
- Refresh токены с долгим временем жизни (7 дней)
- CSRF защита для state-changing операций
- Rate limiting для API
- Валидация всех входных данных

### 6.3 Надёжность
- Резервное копирование БД ежедневно
- Логи всех операций
- Обработка ошибок без краша приложения
- Graceful degradation при недоступности сервисов

### 6.4 Доступность
- WCAG 2.1 Level AA для accessibility
- Поддержка мобильных устройств
- Поддержка современных браузеров (Chrome, Firefox, Safari, Edge)

---

## 7. ФАЗЫ РАЗРАБОТКИ

### Фаза 1: MVP (Minimum Viable Product)
**Срок:** 2 недели

**Функционал:**
- ✅ Модуль `users` (регистрация, вход, профиль)
- ✅ Модуль `catalog` (просмотр товаров, категорий)
- ✅ Модуль `cart` (добавление, удаление, просмотр)
- ✅ Модуль `orders` (оформление заказа)
- ✅ Базовый UI

**Критерии готовности:**
- Пользователь может зарегистрироваться
- Может просматривать товары
- Может добавить в корзину и оформить заказ

---

### Фаза 2: Расширение функционала
**Срок:** 2 недели

**Функционал:**
- ✅ Модуль `payments` (интеграция с платёжной системой)
- ✅ Модуль `reviews` (отзывы и рейтинги)
- ✅ Расширенный поиск и фильтрация
- ✅ История заказов
- ✅ Email уведомления

---

### Фаза 3: Админ-панель
**Срок:** 2 недели

**Функционал:**
- ✅ Модуль `admin` (полнофункциональная админ-панель)
- ✅ Управление товарами и категориями
- ✅ Управление заказами
- ✅ Базовая аналитика
- ✅ Модерация отзывов

---

### Фаза 4: Полировка и оптимизация
**Срок:** 1 неделя

**Функционал:**
- ✅ Оптимизация производительности
- ✅ E2E тесты
- ✅ Документация API
- ✅ Логирование и мониторинг

---

## 8. ТРЕБОВАНИЯ К ТЕСТИРОВАНИЮ

### 8.1 Unit-тесты
- Покрытие бизнес-логики (Domain и Application слои) > 80%
- Тесты для всех Use Cases
- Тесты для валидации

### 8.2 Integration-тесты
- Тесты API endpoints
- Тесты работы с БД
- Тесты интеграций между модулями (Event Bus)

### 8.3 E2E-тесты
- Критические пользовательские сценарии:
  - Регистрация → Вход
  - Просмотр каталога → Добавление в корзину → Оформление заказа
  - Админ: создание товара
- Тесты на основных браузерах

---

## 9. ДОКУМЕНТАЦИЯ

### 9.1 Кодовая документация
- README.md в каждом модуле с описанием
- Docstrings для всех публичных методов
- Комментарии для сложной бизнес-логики

### 9.2 API документация
- Автоматическая генерация через FastAPI Swagger UI
- Описание всех endpoints
- Примеры запросов/ответов

### 9.3 Пользовательская документация
- Руководство пользователя
- Руководство администратора

---

## 10. КРИТЕРИИ ПРИЁМКИ

Проект считается завершённым, когда:

1. ✅ Все модули из Фазы 1-3 реализованы
2. ✅ API покрытие тестами > 70%
3. ✅ E2E тесты проходят для основных сценариев
4. ✅ Документация API completa
5. ✅ Админ-панель функциональна
6. ✅ Процесс покупки работает от начала до конца
7. ✅ Соблюдена модульная архитектура
8. ✅ Нет direct imports между модулями
9. ✅ Event Bus работает корректно

---

## 11. РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Сложность модульной архитектуры | Средняя | Высокое | Чёткое следование ARCHITECTURE_RULES.md |
| Проблемы с Event Bus | Средняя | Высокое | Unit и integration тесты для Event Bus |
| Интеграция платёжной системы | Низкая | Высокое | Mock сервис для разработки |
| Производительность БД | Низкая | Среднее | Индексы, кеширование, оптимизация запросов |

---

## 12. СЛЕДУЮЩИЕ ШАГИ

После утверждения ТЗ:

1. Создать структуру директорий согласно ARCHITECTURE_RULES.md
2. Настроить инфраструктуру (БД, миграции)
3. Реализовать модуль `users` (первый приоритет)
4. Реализовать модуль `catalog`
5. Продолжать по порядку согласно Фазам разработки

---

**Статус:** Ожидает утверждения
**Последнее обновление:** 27.01.2026
