# 🛒 Backend - Интернет-магазин

## Описание проекта

Backend часть интернет-магазина на FastAPI с модульной архитектурой. Проект реализует Domain-Driven Design (DDD) и использует PostgreSQL для хранения данных.

---

## Структура проекта

```
backend/
├── core/                   # Ядро приложения
│   ├── config.py          # Конфигурация
│   ├── database.py        # Подключение к БД
│   └── dependencies.py    # Dependency Injection
│
└── modules/               # Модули доменной области
    ├── catalog/           # Каталог товаров (только чтение)
    └── cart/              # Корзина покупок
```

---

## Модули системы

### 📦 Модуль Catalog (Каталог товаров)

**Назначение:** Просмотр товаров и категорий.

**Основной функционал:**
- Просмотр списка товаров
- Просмотр деталей товара
- Просмотр категорий
- Поиск и фильтрация товаров

**API Endpoints:**
```
GET    /api/products              - Список товаров
GET    /api/products/search       - Поиск товаров
GET    /api/products/:id          - Детали товара

GET    /api/categories            - Список категорий
GET    /api/categories/tree       - Дерево категорий
GET    /api/categories/:id        - Детали категории
```

**Доменные сущности:**
- Product (Товар)
- Category (Категория)

---

### 🛒 Модуль Cart (Корзина покупок)

**Назначение:** Управление корзиной покупок пользователей.

**Основной функционал:**
- Создание корзины для пользователя или гостя
- Добавление/удаление товаров
- Изменение количества товаров
- Расчёт итоговой суммы


**API Endpoints:**
```
GET    /api/cart                  - Получить корзину
POST   /api/cart/items            - Добавить товар
PUT    /api/cart/items/:id        - Изменить количество
DELETE /api/cart/items/:id        - Удалить товар
DELETE /api/cart                  - Очистить корзину

```

**Доменные сущности:**
- Cart (Корзина)
- CartItem (Товар в корзине)

**Особенности:**
- Фиксация цены при добавлении товара
- Проверка доступности товаров

---

## Технологический стек

### Backend
- **Python 3.11+**
- **FastAPI** - веб-фреймворк
- **SQLAlchemy 2.0** - ORM
- **Alembic** - миграции БД
- **PostgreSQL** - база данных
- **Pydantic** - валидация данных

---

## Установка и запуск

### Требования
- Python 3.11+
- PostgreSQL 14+

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Настройка переменных окружения
Создать файл `.env` в корне проекта:
```env
# Database
DATABASE_URL=postgresql://user:password@localhost/shop_db
```

### Миграции базы данных
```bash
# Создание миграции
alembic revision --autogenerate -m "description"

# Применение миграций
alembic upgrade head
```

### Запуск приложения
```bash
# Режим разработки
uvicorn core.app:app --reload --host 0.0.0.0 --port 8000

# Режим продакшена
uvicorn core.app:app --host 0.0.0.0 --port 8000 --workers 4
```

---

## Архитектурные принципы

### Domain-Driven Design (DDD)
- Чёткое разделение на модули по доменным областям
- Доменные сущности (Entities) изолированы от инфраструктуры
- Use Cases описывают бизнес-логику
- Repository Interface абстрагирует доступ к данным

### Dependency Injection
- Использование FastAPI dependencies
- Внедрение зависимостей через функции
- Тестирование через mock зависимости

---

## API Документация

После запуска приложения доступны:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`
- OpenAPI JSON: `http://localhost:8000/openapi.json`

---

## Структура БД

### Основные таблицы
- **products** - товары
- **categories** - категории
- **carts** - корзины
- **cart_items** - товары в корзине

### Индексы
- Индексы на внешние ключи
- Индексы для поиска и фильтрации
- GIN индексы для полнотекстового поиска (PostgreSQL)

---

## Безопасность

### Защита
- Валидация входных данных
- SQL injection защита (SQLAlchemy)
- CORS настройка

---

## Логирование

Логи пишутся в консоль (stdout).

Формат логов:
```
[timestamp] [level] [logger_name] message
```

---


## Контакты и поддержка

**Версия:** 1.0
**Статус:** 📝 В разработке
**Последнее обновление:** 28.01.2026
